from django.utils import timezone
class User_Online_count(APIView):
    permission_classes = []
    def get(self,request):
        ddd = timezone.now().date() - timedelta(days=110)
        count = User.objects.filter(last_login__range=(ddd, timezone.now().date())).count()
        print(count)
        return Response(count)
